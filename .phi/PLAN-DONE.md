# Completed

_Archived from .phi/PLAN.md. Read this when replanning._

- Extend src/tui/theme.rs with the two remaining semantic hues that have no named home yet — a diff hunk-header style (currently Color::Blue, bold) and a role/activity accent for Architecture (Color::Blue) — keeping all existing visuals byte-for-byte identical. Verify: cargo build + `cargo test -p polycode theme` pass; zero raw Color:: inside theme.rs. — M13d3 step 1 done: theme.rs gains STRUCTURE(Blue)+CHIP_FG(Black) named hues and diff_hunk()/activity_accent() helpers; rule() body repaired after an earlier truncation.
- Migrate src/tui/markdown.rs raw colors to theme constants: Cyan->ACCENT, DarkGray->MUTED, Yellow->ATTENTION (inline code), updating its two test assertions to compare against theme-derived styles. Verify: build + markdown tests green; `grep 'Color::' src/tui/markdown.rs` returns nothing. — M13d3 step 2 done: markdown.rs fully migrated — Cyan->theme::ACCENT (fenced code, h1/h2), DarkGray->theme::MUTED (separator + blockquote prefix/italic),...
- Migrate src/tui/mascot.rs raw colors: state_style Idle/Running/Waiting/NeedsUser/Completed/Failed -> ACCENT/MUTED/ATTENTION/SUCCESS/DANGER; accent_style Architecture -> new theme role hue, others -> MUTED. Verify: build + mascot footprint tests green (glyphs/box unchanged); `grep 'Color::' src/tui/mascot.rs` returns nothing. — M13d3 step 3 done: mascot.rs fully migrated — state_style Idle/Running->ACCENT, Waiting->MUTED, NeedsUser->ATTENTION, Completed->SUCCESS, Failed->DANGER;
- Migrate src/tui/render.rs raw colors: diff hunk header (line ~1089 Color::Blue) -> new theme structure style; leave the test-only `Color::Reset` assertion (line ~2143, which asserts an unpainted rail, not a palette choice). Verify: build + render tests green; the only remaining Color:: in src/tui is that intentional Reset assertion. — M13d3 step 4 done: render.rs diff hunk header (diff --git / @@ lines) migrated from raw Color::Blue+BOLD to theme::diff_hunk() (=STRUCTURE blue + BOLD, byte-identical).
